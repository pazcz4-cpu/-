# העלאה לאוויר – מדריך צעד אחר צעד

המדריך הזה מביא אותך ממה שיש עכשיו לכתובת חיה משלך, עם התחברות
אמיתית, נתונים בענן, ואפשרות להוסיף את המערכת למסך הבית בטלפון.

אין צורך בידע טכני. כל שלב הוא העתק–הדבק.

**זמן משוער:** שעה עד שעתיים, רובן המתנה.
**עלות:** הדומיין בלבד (‏40–60 ₪ לשנה). האירוח ובסיס הנתונים חינם
בהתחלה, ומתחילים לעלות רק כשיש הרבה לקוחות.

---

## מה קורה בסוף

| כתובת | מה יש שם | למי |
|-------|----------|-----|
| `setshifts.com` | דף המכירה עם התוכניות והמחירים | לקוחות פוטנציאליים |
| `setshifts.com/app` | המערכת: התחברות, סידור, אילוצים | הלקוחות שלך |
| `setshifts.com/tool` | הכלי המקומי לעסק אחד, בלי חשבונות | אתה, אם תרצה |

---

## שלב 1 · הדומיין ✅ הושלם

**`setshifts.com`** — נרכש.

הדומיין כבר כתוב בקוד: הקישור הקנוני, מפת האתר לגוגל והתצוגה
המקדימה בוואטסאפ ובפייסבוק כולם מצביעים אליו. אין מה לשנות.

> אם תחליף דומיין בעתיד, זה משתנה במקום אחד בלבד: `SITE_URL`
> בראש `build-site.js`.

---

## שלב 2 · לחבר את הקוד ל-Vercel

Vercel הוא מי שמארח את האתר. הוא מתחבר ל-GitHub, וכל פעם שהקוד
מתעדכן הוא מפרסם את הגרסה החדשה לבד.

1. היכנס ל-**vercel.com** ולחץ **Sign Up**.
2. בחר **Continue with GitHub** והרשה גישה.
3. לחץ **Add New… → Project**.
4. ברשימת המאגרים בחר את המאגר של הפרויקט ולחץ **Import**.
5. במסך ההגדרות **אל תשנה כלום** – הפרויקט כבר מכיל את `vercel.json`
   עם כל ההגדרות הנכונות.
6. לחץ **Deploy** וחכה. אחרי כדקה תקבל כתובת זמנית כמו
   `shift-scheduler-abc123.vercel.app`.

**בדוק שזה עובד:** פתח את הכתובת הזמנית. אתה אמור לראות את דף
המכירה. הוסף `/app` לכתובת – אתה אמור לראות מסך התחברות עם שורה
צהובה שכתוב בה "מצב הדגמה". זה תקין, עוד לא חיברנו את השרת.

---

## שלב 3 · לחבר את הדומיין

1. ב-Vercel: **Project → Settings → Domains**.
2. הקלד `setshifts.com` ולחץ **Add**.
3. כשהוא ישאל אם להוסיף גם את `www.setshifts.com` — **כן**. הוא יפנה
   אוטומטית מ-`www` לכתובת הראשית, וזה מה שרצוי: כתובת אחת לגוגל.
4. Vercel יציג לך שתי רשומות DNS — אחת ל-`@` ואחת ל-`www`.
   בפרויקטים חדשים שתיהן מסוג `CNAME` עם אותו ערך, ייחודי לפרויקט
   שלך, בסגנון `<מזהה>.vercel-dns-0NN.com`.

   > **בכוונה אין כאן ערכים לדוגמה.** Vercel שינה את הכתובות שלו
   > בעבר, וערך מועתק ממדריך במקום מהמסך הוא הסיבה הנפוצה ביותר
   > לכישלון בשלב הזה.
   >
   > **ואל תקליד את הערך ביד.** ליד כל ערך במסך של Vercel יש אייקון
   > העתקה (⧉). לחץ עליו והדבק. מחרוזת באורך כזה היא בדיוק המקום
   > שבו טעות של תו אחד מפילה את כל השלב.

5. היכנס לאתר שבו קנית את `setshifts.com`, מצא **DNS** או
   **ניהול רשומות**, והוסף את שתי הרשומות בדיוק כפי ש-Vercel הציג.

   **ב-Cloudflare:** לוח הבקרה הוא `dash.cloudflare.com` — לא
   `developers.cloudflare.com`, שהוא אתר התיעוד ואין בו מה לשנות.
   משם: הדומיין → **DNS** → **Records** → **+ Add record**.
   בטופס יש מתג **Proxy status** שדלוק כברירת מחדל — **כבה אותו**
   בשתי הרשומות (ענן אפור, `DNS only`), כי Vercel דורש
   `Proxy: Disabled`. אם Cloudflare מודיעה שרשומת `CNAME` בשורש
   "תשוטח" (flattened) — זה תקין ורצוי.

   > אם יש שם כבר רשומות `A` או `CNAME` על `@` או `www` מהרשם
   > (דף "בקרוב" וכדומה) – מחק אותן. שתי רשומות סותרות על אותו שם
   > גורמות לאתר לעלות לפעמים ולא לעלות לפעמים.

6. חזור ל-Vercel וחכה. זה לוקח בין כמה דקות לכמה שעות. כשזה מוכן
   תראה **Valid Configuration** ואייקון של מנעול.

תעודת ה-HTTPS (המנעול) מונפקת אוטומטית ומתחדשת לבד. אין מה לעשות.

### אם Vercel מציג "Invalid Configuration"

זה אומר דבר אחד בלבד: Vercel שאל את שרתי ה-DNS של הדומיין מה הכתובת,
והתשובה לא הייתה מה שהוא ביקש. עבור על אלה לפי הסדר — כמעט תמיד זה
אחד מהם:

0. **הרשומות בכלל לא נוצרו.** לפני כל בדיקה אחרת — הסתכל בטבלת
   הרשומות אצל הרשם. אם כתוב בה `No DNS records found` או
   `0 of 200 records`, היא ריקה: אין מה לתקן, צריך פשוט ליצור את
   שתי הרשומות. זה המצב הנפוץ ביותר, והוא נראה בדיוק כמו תקלה.

1. **השוואת ערכים, תו מול תו.** פתח את הרשומה אצל הרשם לצד המסך של
   Vercel. כתובת IP אחת שונה בספרה אחת = Invalid. אל תעתיק ערך משום
   מדריך, גם לא מזה.

2. **Cloudflare עם ענן כתום.** אם הדומיין ב-Cloudflare והרשומה מסומנת
   **Proxied** (ענן כתום), Vercel רואה את הכתובת של Cloudflare ולא את
   שלו. לחץ על הענן כדי שיהפוך לאפור — **DNS only**. זו הסיבה מספר
   אחת אצל מי שמשתמש ב-Cloudflare.

3. **רשומות ישנות שנשארו.** רשם דומיינים מוסיף בעצמו רשומת `A` או
   `CNAME` לדף "בקרוב". מחק כל רשומת `A`, `AAAA` או `CNAME` על `@`
   ועל `www` שאינה של Vercel. שתי רשומות סותרות = תשובה אקראית.

4. **שדה השם.** אצל חלק מהרשמים כותבים `@`, אצל אחרים משאירים ריק.
   אם כתבת `setshifts.com` בשדה השם, ייתכן שהרשם הפך את זה ל-
   `setshifts.com.setshifts.com`. בדוק איך נראית הרשומה אחרי השמירה.

5. **הדומיין עדיין לא אומת.** דומיין `.com` חדש מושעה עד שלוחצים על
   קישור האימות שאיקאן שולחת במייל. בדוק בתיבה, כולל בספאם.

אחרי תיקון — חזור ל-Vercel ולחץ **Refresh**. אם הכול נכון וזה עדיין
אדום, פשוט חכה: שינוי DNS לוקח בין כמה דקות לכמה שעות להתפשט.

**בדוק שזה עובד:** פתח את `https://setshifts.com` — דף המכירה.
`https://setshifts.com/app` — מסך ההתחברות. שלח את הכתובת לעצמך
בוואטסאפ — אמורה להופיע תצוגה מקדימה עם האייקון והכותרת.

---

## שלב 4 · לפתוח בסיס נתונים

עד כאן האתר באוויר, אבל ההתחברות עדיין מדומה – כל דפדפן שומר
לעצמו. עכשיו מחברים שרת אמיתי.

1. היכנס ל-**supabase.com** והירשם (אפשר עם GitHub).
2. לחץ **New Project**.
   - **Name:** שם כלשהו, למשל `shift-scheduler`.
   - **Database Password:** לחץ Generate ו**שמור את הסיסמה** במקום
     בטוח. לא תצטרך אותה ביום-יום, אבל בלעדיה אי אפשר לשחזר.
   - **Region:** בחר **Frankfurt** או **London** – הכי קרוב לישראל
     ולאירופה.
3. חכה כדקה עד שהפרויקט מוכן.

### להריץ את הסכימה

1. בתפריט הצד לחץ **SQL Editor** ואז **New query**.
2. פתח את הקובץ `supabase/schema.sql` מהפרויקט, העתק את **כל** התוכן
   והדבק לעורך.
3. לחץ **Run**.
4. אתה אמור לראות `Success`. אם יש שגיאה – שלח לי אותה, היא תגיד
   בדיוק מה חסר.

> הסקריפט הזה יוצר את הטבלאות ואת כללי ההרשאה שמבטיחים שחברה אחת
> לא יכולה לראות נתונים של חברה אחרת. הכללים נאכפים בבסיס הנתונים
> עצמו, ולא בקוד שרץ בדפדפן – כך שגם פנייה ישירה לשרת לא תעקוף אותם.

### אימות מייל – משאירים דלוק

Supabase שולח מייל אימות לכל נרשם, ועד שלא לוחצים על הקישור אי
אפשר להיכנס. **זה נשאר דלוק.** המוצר נמכר לעסקים במנוי חודשי,
וכתובת מייל שאי אפשר להגיע אליה פירושה חשבונית שנעלמת, התראת
כישלון חיוב שאיש לא רואה, ואיפוס סיסמה בלתי אפשרי. עדיף לסנן
כתובת שגויה בשנייה הראשונה מאשר בחודש השלישי.

המערכת בנויה לזה: ההרשמה נחתכת לשניים, ושם החברה נשמר על משתמש
האימות עצמו (ולא בדפדפן), כך שהחברה נפתחת בכניסה הראשונה שתצליח
— גם אם הלקוח לחץ על הקישור מהטלפון ונרשם מהמחשב. במסך מוצגת
הודעה ירוקה שמפנה לתיבת המייל, ולא שגיאה.

**אבל שני דברים חייבים להיות מוגדרים, אחרת המייל פשוט לא יעבוד:**

#### 1. כתובת החזרה (חובה)

ברירת המחדל של Supabase היא `localhost:3000`, כלומר הקישור במייל
של הלקוח יוביל למחשב שלו ולא לאתר שלך.

**Authentication → URL Configuration:**

| שדה | ערך |
|------|-----|
| Site URL | `https://setshifts.com/app/` |
| Redirect URLs | `https://setshifts.com/**` |

#### 2. שרת מייל משלך (חובה לפני לקוח ראשון)

שירות המייל המובנה של Supabase מיועד לפיתוח בלבד, והוא חסום
בקצב נמוך מאוד — כמה מיילים בשעה. המיילים הראשונים יעברו, ואז
ההרשמות יפסיקו לעבוד בלי שתדע למה.

**Project Settings → Authentication → SMTP Settings** → הדלק
**Enable Custom SMTP** ומלא את הפרטים של ספק מייל. אפשרויות טובות
עם שכבה חינמית: **Resend**, **Brevo**, **Postmark**, **Amazon SES**.

בזמן שאתה שם, שווה גם: **Authentication → Email Templates** —
התבניות באנגלית ובמיתוג Supabase. תרגם אותן לעברית ושים את שם
המוצר. זה המייל הראשון שהלקוח שלך רואה.

> **עד שיש SMTP משלך:** אפשר להתקדם ולבדוק הכול עם החשבון שלך.
> רק אל תשווק ללקוחות לפני שזה מוגדר.

---

## שלב 5 · לחבר את השרת למערכת

צריך שני ערכים מ-Supabase. שניהם נמצאים ב-**Project Settings → API**:

| מה | איפה | מה עושים איתו |
|----|------|----------------|
| **Project URL** | למעלה במסך | נכנס לקובץ `config.js` |
| **anon public** | תחת Project API keys | נכנס לקובץ `config.js` |
| **service_role** | תחת Project API keys | נכנס ל-Vercel, **לא** לקובץ |

> ⚠️ **המפתח `service_role` הוא סוד.** מי שמחזיק בו יכול לקרוא
> ולשנות את הנתונים של **כל** הלקוחות שלך. הוא לעולם לא נכנס לקוד
> ולא לדפדפן – רק למשתני הסביבה של Vercel.
> המפתח `anon` לעומת זאת מיועד לדפדפן ואינו סודי.

### 5א. החיבור ל-Supabase ✅ הושלם

הכתובת והמפתח של הפרויקט כבר בקוד, ב-`build-site.js`:

```js
const SUPABASE_URL = process.env.SUPABASE_URL || 'https://...supabase.co';
const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_...';
```

הם נכתבים ל-`site/app/config.js` בכל פריסה, ולא לקובץ שבמאגר.
ההפרדה הזו מכוונת: `config.js` שבמאגר נשאר ריק, ולכן פתיחה מקומית
של `app.html` והרצת הבדיקות ממשיכות במצב הדגמה ולעולם אינן נוגעות
בנתונים של לקוחות אמיתיים.

> **המפתח הזה אינו סודי.** הוא מיועד לרוץ בדפדפן של כל לקוח, ו-
> Supabase עצמה מגדירה אותו כ"בטוח לשיתוף פומבי". מה שמגן על
> הנתונים הוא כללי ההרשאה שב-`supabase/schema.sql`, שנאכפים בבסיס
> הנתונים עצמו.
>
> **להחלפת מפתח** אין צורך לגעת בקוד: מגדירים `SUPABASE_URL` ו-
> `SUPABASE_ANON_KEY` כמשתני סביבה ב-Vercel, והם גוברים.

---

### 5ב. משתני הסביבה ב-Vercel

**Vercel → Project → Settings → Environment Variables**, והוסף שניים:

| Name | Value |
|------|-------|
| `SUPABASE_URL` | אותה כתובת Project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | המפתח `service_role` |

סמן את שלושת הסביבות (Production, Preview, Development) ולחץ **Save**.

אחרי ההוספה לחץ **Deployments → … → Redeploy** כדי שהמשתנים ייכנסו
לתוקף.

---

## שלב 6 · לבדוק שהכל עובד

1. פתח את `setshifts.com/app`.
2. **השורה הצהובה "מצב הדגמה" צריכה להיעלם.** אם היא עדיין שם,
   `config.js` לא התעדכן – בדוק שדחפת ושהפריסה הסתיימה.
3. לחץ **פתיחת חשבון לעסק**, מלא פרטים והירשם.
4. בנה סידור לשבוע.
5. פתח את אותה כתובת **בטלפון** והתחבר עם אותם פרטים. אתה אמור
   לראות את אותו סידור. זו ההוכחה שהנתונים באמת בענן.
6. בלשונית **משתמשים** צור משתמש לעובד, וקשר אותו לכרטיס עובד.
   התחבר איתו בדפדפן פרטי – הוא אמור לראות רק את המשמרות שלו.

---

## שלב 7 · להוסיף למסך הבית בטלפון

המערכת מותקנת כמו אפליקציה, בלי חנות אפליקציות.

**אייפון (Safari – חייב Safari, לא Chrome):**
1. פתח את `setshifts.com/app`
2. לחץ על כפתור השיתוף (ריבוע עם חץ למעלה)
3. גלול ולחץ **הוספה למסך הבית**
4. לחץ **הוסף**

**אנדרואיד (Chrome):**
1. פתח את `setshifts.com/app`
2. לרוב תקפוץ מאליה הצעה **התקן אפליקציה** – לחץ עליה
3. אם לא: תפריט שלוש הנקודות ← **התקנת אפליקציה**

האייקון יופיע במסך הבית, והמערכת תיפתח במסך מלא בלי שורת הכתובת.

> זה מה שכדאי לשלוח לעובדים: קישור ל-`/app` והוראה להוסיף למסך
> הבית. מבחינתם זו אפליקציה לכל דבר.

---

## מה עוד נשאר

### חיוב אמיתי

**מודל המנוי:** הלקוח פותח מנוי עם כרטיס אשראי כבר בהרשמה, אבל
**14 הימים הראשונים ללא חיוב כלל**. בתום התקופה ספק התשלומים מחייב
אוטומטית, ומשם כל חודש – עד שמבטלים. ביטול בתוך תקופת הניסיון אינו
עולה דבר, והגישה נשמרת עד סוף התקופה.

**מה כבר מוכן:**

- **התנאים גלויים בכל מקום** שבו הלקוח מחליט: דף המכירה, כרטיסי
  התוכניות, מסך ההרשמה (כולל תאריך החיוב המדויק) ולשונית המנוי.
- מצב המנוי (`plan`, `status`, `valid_until`) **נעול לכתיבה מהדפדפן**.
  רק השרת משנה אותו, בתגובה לאירוע מספק התשלומים.
- `api/billing/webhook.js` – קליטת אירועי חיוב: אימות חתימה, מניעת
  עיבוד כפול, ועדכון מצב המנוי. זו הנקודה שבה המנוי הופך למשלם.
- `api/billing/_providers.js` – מתאם לכל ספק. כל ספק מתרגם את האירוע
  שלו למבנה אחיד אחד, ושאר הקוד אינו יודע באיזה ספק מדובר.
- חלון חסד של יומיים בתום הניסיון, כדי שעיכוב ב-webhook לא ינעל
  לקוח שכרטיסו כבר חויב.
- 14 בדיקות לנקודת הקצה, ובהן חתימה מזויפת, גוף ששונה אחרי החתימה,
  ואותו אירוע שנשלח פעמיים.

**מי מבצע את החיוב:** אנחנו, לא הספק. `api/billing/cron.js` רץ
פעם ביום (מוגדר ב-`vercel.json`) ועושה בדיוק את זה:

| מצב | מה קורה |
|-----|----------|
| ניסיון הסתיים, יש כרטיס | חיוב ראשון → מנוי פעיל לחודש |
| מנוי פעיל שהתקופה נגמרה | חידוש חודשי |
| חיוב נכשל | `past_due`, ניסיון חוזר יומי במשך 7 ימי חסד |
| אחרי ימי החסד | המנוי פג |
| בוטל והתקופה נגמרה | נסגר, בלי חיוב |
| ניסיון הסתיים בלי כרטיס | פג, בלי חיוב |

בחרתי שהתזמון יהיה שלנו ולא של PayPlus, כדי ש"14 ימים ללא חיוב"
יהיה התנהגות שאנחנו מבטיחים ולא הימור על יכולת התזמון של הספק.

**הגנה מפני חיוב כפול:** לפני כל חיוב נרשמת תביעה על התקופה
המסוימת הזו. אם הרישום נכשל כי הוא כבר קיים – מישהו כבר חייב,
ומדלגים. זה מחזיק גם אם ה-cron רץ פעמיים, גם אם הוא רץ במקביל,
וגם אם נפל באמצע. אותו מפתח נשלח גם ל-PayPlus.

### מה עוד צריך כדי להפעיל את PayPlus

שלושה דברים שדורשים את התיעוד של PayPlus, ולא כתבתי אותם בניחוש.
הם מסומנים כ-`TODO 1/2/3` בקובץ `api/billing/_providers.js`:

1. **מבנה ההזדהות מול ה-API** – איך בדיוק נשלחים `api_key` ו-`secret_key`,
   והנתיבים המדויקים של יצירת דף תשלום ושל חיוב לפי טוקן.
2. **אלגוריתם החתימה** על ההודעה החוזרת – היא מגיעה בכותרת `hash`,
   וצריך לדעת איזה אלגוריתם, באיזה קידוד, ועל מה חותמים.
3. **שמות השדות בהודעה החוזרת** ומיפוי הסטטוסים שלהם לשלנו.

עד שאלה ימולאו, `PAYPLUS_READY` נשאר כבוי: שום הודעה אינה מתקבלת
ושום כרטיס אינו מחויב. יש בדיקה שמוודאת את זה.

**מה כן עובד כבר עכשיו בלי PayPlus:** ביטול וחידוש מנוי
(`/api/billing/cancel`, `/api/billing/resume`), כי בתכנון הזה הם
רק מסמנים דגל ואינם דורשים את הספק.

**משתני סביבה שיידרשו ב-Vercel:**

| Name | Value |
|------|-------|
| `BILLING_PROVIDER` | `payplus` |
| `BILLING_WEBHOOK_SECRET` | הסוד לאימות החתימה, מלוח הבקרה של PayPlus |
| `CRON_SECRET` | מחרוזת אקראית שתמציא. בלעדיה כל אחד יכול להפעיל סבב חיובים |
| `PUBLIC_BASE_URL` | `https://setshifts.com` |
| `PAYPLUS_API_KEY` | מלוח הבקרה של PayPlus |
| `PAYPLUS_SECRET_KEY` | מלוח הבקרה של PayPlus |
| `PAYPLUS_TERMINAL_UID` | מזהה המסוף |
| `PAYPLUS_PAYMENT_PAGE_UID` | מזהה דף התשלום |
| `PAYPLUS_SANDBOX` | `true` לבדיקות, להסיר בייצור |
| `PAYPLUS_READY` | `true` רק אחרי שהושלמו שלושת ה-TODO |

ואצל PayPlus צריך להגדיר את כתובת ההודעה החוזרת:
`https://setshifts.com/api/billing/webhook`

> **הערה על מכירה בחו״ל:** PayPlus מתאים לישראל. אם בהמשך תמכור
> במדינות נוספות, הבעיה לא תהיה הכרטיס אלא המס – ואז נשקול מחדש
> ספק שמשמש כ-Merchant of Record. המבנה כאן תומך בזה: הוספת ספק
> היא ערך אחד ב-`_providers.js`, ושום קובץ אחר לא משתנה.

### התראות דחיפה

היום ההתראות עובדות רק כשהדפדפן פתוח. להתראות שמגיעות גם כשהוא
סגור צריך שרת התראות. אפשר להוסיף בהמשך.

---

## פתרון תקלות

**"מצב הדגמה" לא נעלם**
`config.js` ריק או שהפריסה לא הסתיימה. בדוק ב-Vercel → Deployments
שהפריסה האחרונה ירוקה, ורענן עם `Ctrl+Shift+R`.

**ההרשמה נכשלת עם "בדקו את תיבת המייל"**
אימות המייל דלוק. כבה אותו לפי שלב 4, או אשר את המייל שנשלח.

**יצירת משתמש נכשלת**
משתני הסביבה ב-Vercel חסרים או שלא עשית Redeploy אחרי שהוספת אותם.

**הדומיין לא עולה אחרי שעה**
בדוק ברשם הדומיין שרשומות ה-DNS נשמרו בדיוק כפי ש-Vercel הציג.
טעות נפוצה: לכתוב `@` בשדה השם כשהרשם מצפה לשם הדומיין המלא, או
להפך.

**עדכנתי קוד ולא רואים שינוי**
ה-Service Worker שומר עותק למקרה שאין קליטה. רענון עם
`Ctrl+Shift+R` פותר. בטלפון: סגור את האפליקציה לגמרי ופתח מחדש.

---

## איפה עולה כסף, ומתי

| שירות | חינם עד | אחר כך |
|-------|---------|--------|
| **Vercel** | 100GB תעבורה בחודש | ‏20$ לחודש |
| **Supabase** | 500MB נתונים, 50,000 משתמשים פעילים בחודש | ‏25$ לחודש |
| **דומיין** | – | ‏40–60 ₪ לשנה |

בפועל: עשרות לקוחות ראשונים נכנסים בנוחות במסגרת החינמית. סידור
משמרות הוא נתונים קטנים מאוד – מדובר בטקסט בלבד.
