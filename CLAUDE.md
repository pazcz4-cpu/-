# SetShifts — הנחיות עבודה

## איך למסור למשתמש דברים שהוא צריך להעתיק

**תמיד בבלוק קוד, לעולם לא כהפניה לקובץ.**

כשצריך שהמשתמש יריץ SQL ב-Supabase, יריץ פקודה, או ידביק ערך
ב-Vercel — יש להדביק את התוכן עצמו בתוך בלוק קוד בתשובה, כדי
שיהיה לו כפתור העתקה. משפט כמו "הרץ מחדש את `supabase/schema.sql`"
מחייב אותו לפתוח את הקובץ ולהעתיק ידנית, וזה בדיוק מה שנופל.

זה חל גם על פקודות ארוכות ועל קטעי SQL של מאה שורות.

## מה לא לשים בצ'אט ולא ברפוזיטורי

- `SUPABASE_SERVICE_ROLE_KEY` ומפתחות סודיים של Supabase
- מפתח ה-API של Resend
- טוקנים של Cloudflare ושל Vercel
- פרטי PayPlus של הסביבה החיה
- מפתחות חתימה של Apple, מפתח APNs, וקרדנציאלס של Firebase

כל אלה עוברים ישירות מהמקור אל Vercel או אל שירות הבנייה.
ערכים ציבוריים (Project URL, publishable key, MAIL_FROM, APP_URL)
מותרים.

כשמבקשים מהמשתמש להעתיק SQL או לוג בנייה — להזכיר לו לא להדביק
ערכים של משתני סביבה.
